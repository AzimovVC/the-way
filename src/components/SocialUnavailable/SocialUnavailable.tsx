/**
 * Что стоит на социальных экранах, когда сервера нет вовсе (`configured === false` — нет ключей
 * в `.env.local`). Это единственный оставшийся случай: вход теперь спрашивается
 * [дверью перед приложением](../AuthGate/AuthGate.tsx), поэтому дальше неё невошедшего не бывает,
 * и «войди, чтобы увидеть друзей» на этих экранах было бы текстом, который никто не прочтёт.
 *
 * Дверь тут не предлагается нарочно: запертая дверь и дверь в стене — разные беды, и звать в
 * аккаунт на сборке, у которой нет сервера, значит обещать то, чего в ней не построено.
 */
export default function SocialUnavailable() {
  return (
    <p className="text-[13px] text-text-muted">
      Сервер не подключён, и друзей в этой сборке нет. Дорога работает как обычно — она лежит на
      этом телефоне.
    </p>
  )
}
